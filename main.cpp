#include "filemap.h"

#include "vulkanapp.h"

#include "terrain.h"

int main() {
  test_terrain();

  // VulkanApp app;

  // try {
  //   app.run();
  // } catch (const std::runtime_error& e) {
  //   std::cerr << e.what() << std::endl;
  //   return EXIT_FAILURE;
  // }

  return EXIT_SUCCESS;
}

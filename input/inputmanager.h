/**
 * inputmanager.h - Manager for all inputs for all players.
 **/
 #pragma once

 class InputManager {
public:
   InputManager();
   ~InputManager();

  private:
    // consider making a singleton
 };
